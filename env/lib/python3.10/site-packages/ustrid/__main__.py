# File generated by Setupinit
import sys
from ustrid import ustrid


HELP_TEXT = """\
Uniquely generated string identifiers

Usage:
    ustrid
    ustrid <integer>
    ustrid <option>
    
Options:
    -h, --help                      Show help text

Visit the webpage: https://github.com/pyrustic/ustrid
"""


def main():
    n = 1
    if len(sys.argv) == 2:
        arg = sys.argv[1].lower()
        if arg in ("-h", "--help"):
            print(HELP_TEXT)
            return
        try:
            n = abs(int(arg))
        except ValueError as e:
            print("Bad request")
            return
    elif len(sys.argv) > 2:
        print("Bad request")
        return
    for _ in range(0, n):
        print(ustrid())


if __name__ == "__main__":
    main()
